//! Tool upgrade helpers — watering area and stamina cost.
//!
//! These functions have been moved to `crate::shared` so that all domains
//! can access them without cross-domain imports. This module is kept as a
//! placeholder so the `pub mod tool_upgrades;` declaration still compiles.
