# Hearthfield Spec Entry Point

This file exists as the playbook-compatible entry point for workflows that expect `docs/spec.md`.

The canonical top-level product specification for this repository is:

- [`GAME_SPEC.md`](../GAME_SPEC.md)

Use `GAME_SPEC.md` as the authoritative full project spec. Do not maintain a second summarized spec here that can drift.
