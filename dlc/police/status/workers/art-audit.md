# Precinct Art Audit

Scope:
- Read all `dlc/police/src/domains/*/mod.rs`.
- Read all `dlc/police/src/domains/ui/*.rs`.
- Checked [`main.rs`](/home/geni/swarm/hearthfield/dlc/police/src/main.rs#L25) and [`shared/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/shared/mod.rs#L20) only to confirm boot state and asset mounting assumptions.

Legend:
- `WORKING`: concrete authored path exists in code.
- `PLACEHOLDER`: partially authored, but still falls back to flat-color blocks or other stand-ins in the audited journey.
- `MISSING`: no concrete implementation found.

| Step | Status | Evidence | Notes |
| --- | --- | --- | --- |
| 1. Boot -> MainMenu (styled?) | `WORKING` | `GameState` defaults to `Loading` in [`shared/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/shared/mod.rs#L20), `UiPlugin` sends `Loading -> MainMenu` via `boot_to_main_menu` in [`ui/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/mod.rs#L20) and [`ui/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/mod.rs#L123), and `spawn_main_menu` builds a dark full-screen layout with colored buttons and title text in [`ui/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/mod.rs#L281). | Styled UI is present, but it is color/text driven rather than sprite-backed. |
| 2. New Game -> Playing | `WORKING` | Pressing the `New Game` button sets `NextState(GameState::Playing)` in [`ui/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/mod.rs#L383). | End-to-end menu transition exists. |
| 3. Player sprite visible (not colored rect)? | `WORKING` | `spawn_player` requests `player_officer.png` via atlas sprite creation in [`player/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/player/mod.rs#L141) and [`player/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/player/mod.rs#L348). The blue `Sprite::from_color` path is only the fallback in [`player/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/player/mod.rs#L146). | Authored sprite path exists for the player, so the intended path is a sprite sheet, not a rect. |
| 4. Map tiles use atlas sprites? | `PLACEHOLDER` | Interior precinct tiles load `tilesets/precinct_office.png` and build atlas sprites in [`world/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/world/mod.rs#L325), [`world/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/world/mod.rs#L455), and [`world/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/world/mod.rs#L627). But the non-interior branch still spawns flat `Sprite::from_color` tiles in [`world/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/world/mod.rs#L483). | Precinct interior is atlas-based; exterior road/grass/sidewalk tiles are still colored blocks. |
| 5. NPC sprites loaded? | `WORKING` | NPC spawning uses `npc_sprite(...)` first in [`npcs/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/npcs/mod.rs#L848), sprite sheets are defined for authored NPC ids in [`npcs/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/npcs/mod.rs#L1131), and sprite construction uses `Sprite::from_atlas_image` in [`npcs/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/npcs/mod.rs#L1054). | The fallback role-colored rect still exists, but the authored path for the shipped NPC list is sprite-sheet based. |
| 6. Precinct objects have sprites? | `WORKING` | Every precinct interactable spec includes an `atlas_index` in [`precinct/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/precinct/mod.rs#L74), the office furniture atlas is loaded in [`precinct/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/precinct/mod.rs#L430), and objects spawn with atlas sprites in [`precinct/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/precinct/mod.rs#L377) and [`precinct/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/precinct/mod.rs#L457). | Intended path is sprite-backed props, with color fallback only if atlas setup is unavailable. |
| 7. Toast notifications visible? | `WORKING` | Notification overlay spawns at startup in [`ui/notifications.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/notifications.rs#L93), toast cards are created from `ToastEvent` in [`ui/notifications.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/notifications.rs#L300), and those cards have background, border, and text styling in [`ui/notifications.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/notifications.rs#L312). | Toasts are real visible UI, not just events with no renderer. |
| 8. HUD styled? | `WORKING` | `spawn_hud` creates a styled top bar plus fatigue/stress panels and a gold panel in [`ui/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/mod.rs#L406). Meter colors and fills are authored in [`ui/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/mod.rs#L259) and updated live in [`ui/mod.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/mod.rs#L613). | HUD is styled, but like the menu it is UI-color based rather than sprite-decorated. |

## Extra Notes

- Interrogation art is still `PLACEHOLDER`: the interrogation screen renders a framed box with literal `PORTRAIT` text in [`ui/screens.rs`](/home/geni/swarm/hearthfield/dlc/police/src/domains/ui/screens.rs#L766).
- Runtime asset-root caveat: the police binary mounts a relative `assets` directory in [`main.rs`](/home/geni/swarm/hearthfield/dlc/police/src/main.rs#L27), while the authored police art currently lives under `dlc/police/assets/`. The audited domain code is clearly sprite/atlas-oriented, but actual runtime loading depends on how this binary is launched.
